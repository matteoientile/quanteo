[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "quanteo"
version = "0.1.0"
description = "A quantitative options pricing and risk library."
dependencies = [
    "numpy",
    "scipy"
]